<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eoe.drugstore">

    <!-- 各个权限声明 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.FragmentExerciseActivity">
            <intent-filter>
                <action android:name="dian.wo.ba" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver android:name=".BroadCastReceiver.MyReceiver">
            <intent-filter android:priority="20">
                <action android:name="com.eoe.drugstore.fragment.communityFragment" />
            </intent-filter>
        </receiver>
        <receiver android:name=".BroadCastReceiver.MyReceiver2">
            <intent-filter>
                <action android:name="com.eoe.drugstore.fragment.communityFragment" />
            </intent-filter>
        </receiver>

        <activity android:name=".activity.MultiThreadClientActivity" />
        <activity android:name=".activity.GetUrlActivity" />
        <activity android:name=".activity.CodeViewActivity" />
        <activity android:name=".activity.BitmapTest" />
        <activity android:name=".activity.CanvasActivity" />
        <activity android:name=".activity.PathTestActivity" />
        <activity android:name=".activity.ViewAnimatorActivity">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="lee" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DataTypeActivity" />
        <activity android:name=".activity.FrameActivity" />
        <activity android:name=".activity.MyViewActivity" />
        <activity
            android:name=".activity.SchemeActivity"
            android:label="指定scheme的Activity">
            <intent-filter>
                <action android:name="xx" />

                <category android:name="android.intent.category.DEFAULT" />
                <!-- 只要Intent的Data属性的scheme是lee，即可启动该Activity -->
                <data android:scheme="lee" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.BlastActivity" />
        <activity android:name=".activity.PathTextActivity" />
        <activity android:name=".activity.DrawViewAcitivity" />
        <activity android:name=".activity.HandDrawAcitivy" />
        <activity android:name=".activity.PinBallActivity" />
        <activity android:name=".activity.PictureProgressActivity" />
        <activity android:name=".activity.HttpIntenetActivity" />
        <activity android:name=".activity.URLTest" />
        <activity android:name=".activity.GetPostMain" />
        <activity android:name=".activity.MultiThreadDown" />
        <activity android:name=".activity.OkhttpCacheActivity" />
        <activity android:name=".activity.WebGetActivity" />
        <activity android:name=".activity.AActivity" />
        <activity
            android:name=".activity.BActivity"
            android:launchMode="singleTask" />
        <activity android:name=".activity.CActivity" />
        <activity android:name=".activity.LeakageActivity" />
        <activity android:name=".activity.ServiceActivity" />

        <service
            android:name=".service.ForeService"
            android:enabled="true"
            android:exported="true" />
        <service android:name=".service.FirstService" />
        <service android:name=".service.MyIntentService" />

        <activity android:name=".activity.SeeProcessActivity" />
        <activity android:name=".activity.JsonAcitvity" />
        <activity android:name=".activity.ListViewActivity"></activity>
    </application>

</manifest>